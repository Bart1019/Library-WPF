<UserControl x:Class="Library.UI.Views.UserListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.UI.Views"
             xmlns:viewModel ="clr-namespace:Library.Logic.ViewModels;assembly=Library.Logic"
             xmlns:data="clr-namespace:Library.Data.Models;assembly=Library.Data"
             xmlns:v="clr-namespace:Library.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Orientation="Vertical">
        <Label Content="List of Persons:" FontSize="18" FontWeight="Bold" Margin="7 0 10 0" Grid.Column="0"/>
        <Grid>
            <ComboBox x:Name="UserComboBox" ItemsSource="{Binding Users}" Margin="10 0 10 0" HorizontalAlignment="Left" Width="120">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path=FullName}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>
        <ListBox SelectedItem="{Binding SelectedUser}" Height="100" Margin="10,0,10,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding Path=Id, ElementName=UserComboBox}"/>
                        <TextBlock Text="{Binding Path=FullName, ElementName=UserComboBox}"/>
                        <TextBlock Text="{Binding Path=AmountOfBooksRented, ElementName=UserComboBox}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Margin="10,0,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Width ="100" Content="Add User" Margin="4 0 0 0" HorizontalAlignment="Left" Grid.Column="0" Command="{Binding AddCommand}"/>
            <Button Width ="100" Content="Delete User" Margin="20 0 0 0" HorizontalAlignment="Left" Grid.Column="1" Command="{Binding DeleteCommand}" />
            <Button Width ="100" Content="Edit User" Margin="20 0 0 0"  HorizontalAlignment="Left" Grid.Column="2" Command="{Binding EditCommand}"/>
        </Grid>
    </StackPanel>
</UserControl>
